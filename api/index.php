<?php

declare(strict_types=1);

// Vercel serverless entry point. The main application remains at the project
// root so the same code continues to run unchanged under XAMPP.
require dirname(__DIR__) . '/index.php';
